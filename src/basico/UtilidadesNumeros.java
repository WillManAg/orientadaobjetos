package basico;

public class UtilidadesNumeros {

	// recibe el texto y devuelve si el numero es o no es par
	public static boolean esPar(int numero) {
		// si se cumple devuelve true y si no se cumple devuelve false
		return numero % 2 == 0;
		
	}

}
